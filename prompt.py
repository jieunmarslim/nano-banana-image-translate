def get_prompt(target_language: str, source_language: str):
    prompt = f"Translate the text in this {source_language} marketing product image to {target_language}. Text position stays where it is and don't touch anything else other then text. Critical: Only the text should be translated in {target_language} for the output image nothing else changes.CRITICAL WARNING - NEVER SQUASH OR DISTORT Image Chunk's own ratio.eg. Don't squash the character or item to loose it's original ratio. Don't add any extra texts if there wasn't any texts. Dont ruin the original design other than texts."
    return prompt
